export default {
  getClientWidth (selector) {
    return document.querySelector(selector).clientWidth
  },
  getClientHeight (selector) {
    return document.querySelector(selector).clientHeight
  }
}
